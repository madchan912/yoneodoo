# 요너두 — 계획 (리팩토링 + v1.5 방향)

**사실 관계**는 `CONTEXT.md`, **바로 할 일**은 `TASK.md`와 함께 읽으세요.

## 원칙

**v1.5 전에 빅뱅 리팩토링은 하지 않는다.**  
환경변수·API 계약·UI 최소 분리 같은 **얇은 기반**만 깔고 v1.5 기능을보내 속도와 운영 리스크의 균형을 맞춘다.

권장 순서:

1. **얇은 기반** — env URL, 문서·스크립트 정합, API 위생 최소치.
2. **v1.5 기능** — 요리명 검색 UX + 재료 지능화(아래 백로그).
3. **심화 리팩토링** — 전면 DTO, 전역 예외 처리, 캐시 무효화 전략, 쓰기 경로 인증.

---

## v1.5 백로그 (합의된 방향)

### 1) 검색 UX —「요리명」모드

- **토글**: 「재료로 찾기」 vs 「요리명으로 찾기」.
- 요너두 정체성은 **재료 우선**; 요리명 검색은 **추가** 레이어.
- 결과 카드: **핵심 재료를 해시태그**(`#스팸`, `#김치`)로 노출해, 재료가 없을 때 사용자가 타협하며 고를 수 있게 함.

**엔지니어링 함의:** 제목/요리명 검색용 **신규 또는 확장 API 계약**, 태그용 **안정적인 응답 형태**(초기에는 기존 `ingredients`에서 파생 가능).

### 2) 재료 지능화 — v1.5에서는「카테고리 우선」을 피하기

논의된 두 축:

| 접근 | v1.5 적합도 |
|------|----------------|
| **마스터 재료 + 승인 워크플로** | **1차로 권장**: 정규명·별칭·미검토 큐·사람 승인 → 품질·통제에 유리. |
| **넓은 카테고리 + 유사 매칭** | **정규명 위에 나중에 얹기** 좋음; 데이터 없이 튜닝하기 어려움. |

**v1.5 권장 경로:**

- **재료 마스터**(정규명 + `aliases[]` + 상태: `ACTIVE` / `PENDING` / `REJECTED`).
- 크롤러/API 적재 시: 알려진 재료로 **해석(resolve)** 하거나, 미지면 **검토 큐**에 적재; 레시피는 해석된 ID로 `SUCCESS` 또는 **대기/부분** 정책(기획 결정).
- 나중에 재료 JSON에 **`category` 필드**만 추가하는 식으로 확장 가능 — 처음부터 RDB「재료 그래프」 대공사는 피할 수 있음.

### 3) 운영 모델

- 초기: 시드 사전·검토 큐로 **수동 비중 큼**.
- 이후: 관리자 UI 또는 CSV/간단 API로 승인; 사전이 커질수록 자동 비중 증가.

### 4) MVP Admin — 수동 운영용 관리자 도구 (시크릿 기반)

- **권한 테이블 없음**: DB 역할 테이블 대신 환경변수 **`ADMIN_SECRET`**(Spring: `yoneodoo.admin.secret`)과 HTTP 헤더 **`X-Admin-Secret`**로 `/api/v1/admin/**`만 보호한다.
- **API**: `AdminSecretAuthFilter` + `AdminController`(대시보드 통계, 레시피 목록 필터, 미분류 재료 빈 목록 등 확장 예정).
- **Web**: `/admin` — 로그인 화면에서 시크릿 입력 → **sessionStorage** 저장 → `adminClient`(axios)가 모든 어드민 요청에 헤더를 실어 보냄. Vercel/로컬 모두 **운영 시크릿은 서버·Render 환경변수로만** 관리하고, 프론트 저장소에는 넣지 않는다.
- **보안 주의**: 시크릿은 HTTPS + 짧은 세션·IP 제한·레이트 리밋 등을 단계적으로 보강한다. (크롤러용 `POST /recipes`와 별도 이슈)

---

## 리팩토링 테마 (우선순위)

### P0 — 안전·일관 (파급 작게)

- `web`·`data`의 **API 베이스 URL**을 env로 통일.
- **CORS**: 프로필·허용 오리진 목록 등 **한 전략**으로; 컨트롤러별 드리프트 제거.
- **문서**: README 실행 명령·환경변수명을 Vite + Spring 실제와 맞춤.

### P1 — v1.5 확장성

- **`App.jsx` 분리**: API 클라이언트 모듈, 검색/냉장고/레시피 훅, 프레젠테이션 컴포넌트.
- 레시피 저장 후 **재료 캐시 갱신**(`RecipeService` 이후 `IngredientSearchService` 재구축 또는 공통 메서드).
- 검색·목록 페이로드 변경 시 **공개 GET 응답 DTO** 도입(브레이킹 최소화).

### P2 — 품질·스케일

- `@ControllerAdvice` + JSON 에러 바디.
- 쓰기 DTO에 Bean Validation.
- `POST /api/v1/recipes` 보호(토큰·시크릿 헤더·네트워크 제한).
- 구조화 로깅, 기본 메트릭.

---

## v1.5 범위 밖 (명시적으로 끌어오지 않는 한)

- 풀 소셜 로그인(v2 로드맵 — 루트 README).
- **엔터프라이즈급 Admin**(다중 관리자·RBAC·감사 로그 전부) — 현재는 **시크릿 기반 MVP Admin**으로 수동 운영을 우선한다.

---

*백로그 우선순위나 기술 전략이 바뀌면 이 파일을 업데이트하세요.*
